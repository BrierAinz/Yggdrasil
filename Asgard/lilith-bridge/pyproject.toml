[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lilith-bridge"
version = "1.0.0"
<<<<<<< HEAD
description = "Bridge between Lilith and external services (Telegram, Discord)"
requires-python = ">=3.11"
license = "MIT"
dependencies = ["lilith-core", "lilith-api"]

[project.urls]
Repository = "https://github.com/BrierAinz/Yggdrasil"
=======
description = "Bidirectional gateway connecting Yggdrasil to Hermes Agent via MCP and HTTP"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "lilith-core>=2.0.0",
    "lilith-memory>=2.0.0",
    "lilith-skills>=1.0.0",
    "lilith-tools>=2.0.0",
    "lilith-orchestrator>=2.0.0",
    "fastapi>=0.100",
    "uvicorn[standard]>=0.23",
    "pydantic>=2.0",
    "httpx>=0.27",
    "orjson>=3.9",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.1",
]

[project.scripts]
lilith-bridge = "lilith_bridge.__main__:main"

[project.urls]
Homepage = "https://github.com/BrierAinz/Yggdrasil"
Repository = "https://github.com/BrierAinz/Yggdrasil/tree/main/Asgard/lilith-bridge"

[tool.uv.sources]
lilith-core = { workspace = true }
lilith-memory = { workspace = true }
lilith-skills = { workspace = true }
lilith-tools = { workspace = true }
lilith-orchestrator = { workspace = true }
>>>>>>> origin/main
